schema: spec-driven

context: |
  Project name: escanner productos
  Tech stack: Undetermined (empty project — no source files detected)
  Languages: None detected
  Framework: None detected
  Architecture: Not yet established
  Testing: No test runner or test files detected
  Style: Not yet established

rules:
  proposal:
    - Include rollback plan for risky changes
    - Define scope clearly since the project is still being bootstrapped
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns once the stack is chosen
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: ""
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
